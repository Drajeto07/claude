/** The page's size comes from the backend's render specification
 * (formatting/render_spec.py) with each document's settings -- the editor, the
 * template preview and both exports share that one table. This only converts. */
export const PX_PER_MM = 96 / 25.4;
