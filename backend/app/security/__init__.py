"""Security hardening (корекции.docx §33, Phase 15): what uploaded files really
are (files.py), how often a client may call what (rate_limit.py), and the
headers every response carries (headers.py)."""
